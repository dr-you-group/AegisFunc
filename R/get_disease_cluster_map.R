get_disease_cluster_map <- function(input,
                                    ...) {
  map <- get_leaflet_map(input)

  map
}

# get_disease_cluster_map(input)
# map <- get_disease_cluster_map(input)
