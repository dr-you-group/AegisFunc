get_disease_cluster_table <- function(input,
                                      ...) {
  table <- get_cohort_analysis_table(input)

  table
}

# get_disease_cluster_table(input)
# table <- get_disease_cluster_table(input)
